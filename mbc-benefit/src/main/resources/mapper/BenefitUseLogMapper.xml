<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartretail.mbc.benefit.mapper.BenefitUseLogMapper">

    <resultMap id="BaseResultMap" type="com.smartretail.mbc.benefit.entity.BenefitUseLog">
        <id column="id" property="id"/>
        <result column="use_no" property="useNo"/>
        <result column="member_id" property="memberId"/>
        <result column="benefit_type" property="benefitType"/>
        <result column="benefit_id" property="benefitId"/>
        <result column="use_status" property="useStatus"/>
        <result column="order_no" property="orderNo"/>
        <result column="order_amount" property="orderAmount"/>
        <result column="benefit_value" property="benefitValue"/>
        <result column="used_points" property="usedPoints"/>
        <result column="store_code" property="storeCode"/>
        <result column="pos_code" property="posCode"/>
        <result column="operator" property="operator"/>
        <result column="lock_time" property="lockTime"/>
        <result column="confirm_time" property="confirmTime"/>
        <result column="return_time" property="returnTime"/>
        <result column="return_reason" property="returnReason"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="create_by" property="createBy"/>
        <result column="update_time" property="updateTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="is_deleted" property="isDeleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, use_no, member_id, benefit_type, benefit_id, use_status, order_no, order_amount,
        benefit_value, used_points, store_code, pos_code, operator, lock_time, confirm_time,
        return_time, return_reason, remark, create_time, create_by, update_time, update_by, is_deleted
    </sql>

    <select id="selectByOrderNo" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_benefit_use_log
        WHERE order_no = #{orderNo}
          AND is_deleted = 0
        ORDER BY lock_time DESC
    </select>

    <select id="selectLockedWithinMinutes" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_benefit_use_log
        WHERE use_status = 1
          AND lock_time IS NOT NULL
          AND lock_time &lt;= DATE_SUB(NOW(), INTERVAL #{minutes} MINUTE)
          AND is_deleted = 0
        ORDER BY lock_time ASC
    </select>

</mapper>
